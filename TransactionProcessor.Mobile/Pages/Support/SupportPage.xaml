<?xml version="1.0" encoding="utf-8" ?>
<common:NoBackWithoutLogoutPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                               xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                               x:Class="TransactionProcessor.Mobile.Pages.Support.SupportPage"
                               xmlns:common="clr-namespace:TransactionProcessor.Mobile.Pages.Common"
                               xmlns:controls="clr-namespace:TransactionProcessor.Mobile.Controls"
                               xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
                               Shell.NavBarIsVisible="False"
                               x:Name="Support">
    <ScrollView BackgroundColor="{StaticResource pageBackgroundColor}">
        <VerticalStackLayout Spacing="0">

            <!-- Header -->
            <Frame Style="{StaticResource PageHeaderStyle}">
                <Label Text="{Binding Title}" Style="{StaticResource HeaderTitleStyle}" AutomationId="{Binding Title}"/>
            </Frame>

            <!-- Support content -->
            <VerticalStackLayout Padding="16,16,16,16" Spacing="12">

                <!-- App info card -->
                <Frame Style="{StaticResource SectionCardStyle}">
                    <VerticalStackLayout Spacing="8">
                        <Label Text="App Information" FontSize="14" FontAttributes="Bold"
                               TextColor="{StaticResource labelTextColor}"/>
                        <Label Text="{Binding ApplicationName, Mode=OneTime}"
                               TextColor="{StaticResource DarkGray}" FontSize="14"/>
                        <Label Text="{Binding Platform, Mode=OneTime}"
                               TextColor="{StaticResource DarkGray}" FontSize="14"/>
                        <Label Text="{Binding NumberTransactionsStored, Mode=OneTime}"
                               TextColor="{StaticResource DarkGray}" FontSize="14"/>
                    </VerticalStackLayout>
                </Frame>

                <!-- Support action tiles -->
                <Grid ColumnDefinitions="*,*" ColumnSpacing="10">
                    <Frame Grid.Column="0" Style="{StaticResource ActionTileStyle}"
                           BackgroundColor="{StaticResource support}">
                        <Frame.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding UploadLogsCommand}"/>
                        </Frame.GestureRecognizers>
                        <Label Text="Upload Logs" Style="{StaticResource TileLabelStyle}" FontAttributes="Bold" AutomationId="UploadLogsButton"/>
                    </Frame>

                    <Frame Grid.Column="1" Style="{StaticResource ActionTileStyle}"
                           BackgroundColor="{StaticResource support}">
                        <Frame.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding ViewLogsCommand}"/>
                        </Frame.GestureRecognizers>
                        <Label Text="View Logs" Style="{StaticResource TileLabelStyle}" FontAttributes="Bold" AutomationId="ViewLogsButton"/>
                    </Frame>
                </Grid>

                <Frame Style="{StaticResource ActionTileStyle}"
                       BackgroundColor="{StaticResource MidGray}">
                    <Frame.GestureRecognizers>
                        <TapGestureRecognizer Command="{Binding BackButtonCommand}"/>
                    </Frame.GestureRecognizers>
                    <Label Text="Back" Style="{StaticResource TileLabelStyle}" FontAttributes="Bold" AutomationId="BackButton"/>
                </Frame>

            </VerticalStackLayout>
        </VerticalStackLayout>
    </ScrollView>
</common:NoBackWithoutLogoutPage>
