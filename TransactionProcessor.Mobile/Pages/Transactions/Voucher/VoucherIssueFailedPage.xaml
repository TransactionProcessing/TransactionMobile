<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             x:Class="TransactionProcessor.Mobile.Pages.Transactions.Voucher.VoucherIssueFailedPage"
             Shell.NavBarIsVisible="False"
             Padding="0"
             x:Name="VoucherIssueFailed">
    <ScrollView>
        <VerticalStackLayout Spacing="0">

            <!-- Header with gradient background -->
            <Grid HeightRequest="130">
                <Grid.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="{StaticResource voucher}" Offset="0.0"/>
                        <GradientStop Color="#5a2f8a" Offset="1.0"/>
                    </LinearGradientBrush>
                </Grid.Background>
                <VerticalStackLayout VerticalOptions="Center" HorizontalOptions="Center" Spacing="4" Padding="20">
                    <Label Text="{Binding Title}"
                           AutomationId="{Binding Title}"
                           FontSize="20"
                           FontAttributes="Bold"
                           TextColor="White"
                           HorizontalOptions="Center"/>
                </VerticalStackLayout>
            </Grid>

            <!-- Status card -->
            <VerticalStackLayout Padding="20,20,20,16" Spacing="12">
                <Frame Style="{StaticResource StatusCardFrame}">
                    <VerticalStackLayout Spacing="8" HorizontalOptions="Center">
                        <Image Source="paymentfailure.svg" VerticalOptions="Start" Style="{DynamicResource PaymentImage}" HorizontalOptions="Center"/>
                        <Label Style="{StaticResource StatusMessageLabel}"
                               Text="Unfortunately, we could not process your voucher issue, this may be for a variety of reasons.  If you wish you can retry the transaction."/>
                    </VerticalStackLayout>
                </Frame>

                <Button x:Name="CancelButton" Text="Cancel"
                        Style="{DynamicResource VoucherButtonStyle}" AutomationId="CancelButton">
                    <Button.Behaviors>
                        <toolkit:EventToCommandBehavior
                            EventName="Clicked"
                            Command="{Binding CancelledCommand}" BindingContext="{Binding Source={x:Reference VoucherIssueFailed}, Path=BindingContext}" />
                    </Button.Behaviors>
                </Button>
            </VerticalStackLayout>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>