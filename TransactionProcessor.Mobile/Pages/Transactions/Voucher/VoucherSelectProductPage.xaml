<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TransactionProcessor.Mobile.Pages.Transactions.Voucher.VoucherSelectProductPage"
             Shell.NavBarIsVisible="False"
             Padding="0">
    <ScrollView>
        <VerticalStackLayout Spacing="0">

            <!-- Header with gradient background -->
            <Grid HeightRequest="130">
                <Grid.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="{StaticResource voucher}" Offset="0.0"/>
                        <GradientStop Color="#5a2f8a" Offset="1.0"/>
                    </LinearGradientBrush>
                </Grid.Background>
                <VerticalStackLayout VerticalOptions="Center" HorizontalOptions="Center" Spacing="4" Padding="20">
                    <Label Text="{Binding Title}"
                           AutomationId="{Binding Title}"
                           FontSize="20"
                           FontAttributes="Bold"
                           TextColor="White"
                           HorizontalOptions="Center"/>
                    <Label Text="Select a product to continue"
                           FontSize="13"
                           TextColor="White"
                           Opacity="0.85"
                           HorizontalOptions="Center"/>
                </VerticalStackLayout>
            </Grid>

            <!-- Product tiles -->
            <VerticalStackLayout Padding="20,20,20,16" Spacing="16">
                <FlexLayout x:Name="ProductsList"
                            Wrap="Wrap"
                            JustifyContent="SpaceEvenly"
                            AlignItems="Start"/>
                <Button Text="Back"
                        AutomationId="BackButton"
                        Style="{StaticResource StandardButton}"
                        Command="{Binding BackButtonCommand}"/>
            </VerticalStackLayout>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>