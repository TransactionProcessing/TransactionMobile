<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls="clr-namespace:TransactionProcessor.Mobile.Controls"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             x:Class="TransactionProcessor.Mobile.Pages.Transactions.MobileTopup.MobileTopupSuccessPage"
             Shell.NavBarIsVisible="False"
             x:Name="MobileTopupSuccess">
    <Grid AutomationId="MobileTopupSuccessful">
        <ScrollView BackgroundColor="{StaticResource pageBackgroundColor}">
        <VerticalStackLayout Spacing="0">

            <!-- Header -->
            <Frame Style="{StaticResource PageHeaderStyle}" BackgroundColor="{StaticResource successGreen}">
                <Label Text="{Binding Title}" Style="{StaticResource HeaderTitleStyle}"/>
            </Frame>

            <VerticalStackLayout Padding="16,24,16,16" Spacing="16">

                <!-- Success card -->
                <Frame Style="{StaticResource SectionCardStyle}">
                    <VerticalStackLayout Spacing="12" HorizontalOptions="Center">
                        <Image Source="paymentsuccess.png" VerticalOptions="Start"
                               HeightRequest="120" WidthRequest="120"
                               HorizontalOptions="Center"/>
                        <Label Text="Topup Successful!"
                               FontSize="20" FontAttributes="Bold"
                               TextColor="{StaticResource successGreen}"
                               HorizontalTextAlignment="Center"/>
                        <Label Text="Thank you for your topup. An automated payment receipt will be sent to the email address entered."
                               FontSize="14" TextColor="{StaticResource DarkGray}"
                               HorizontalTextAlignment="Center"
                               Margin="8,0"/>
                    </VerticalStackLayout>
                </Frame>

                <!-- Action tiles row -->
                <Grid ColumnDefinitions="*,*" ColumnSpacing="10">
                    <Frame Grid.Column="0" Style="{StaticResource ActionTileStyle}"
                           BackgroundColor="{StaticResource successGreen}"
                           AutomationId="CompleteButton">
                        <Frame.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding CompletedCommand}"/>
                        </Frame.GestureRecognizers>
                        <Label Text="Complete" Style="{StaticResource TileLabelStyle}" FontAttributes="Bold"/>
                    </Frame>

                    <Frame Grid.Column="1" Style="{StaticResource ActionTileStyle}"
                           BackgroundColor="{StaticResource MidGray}">
                        <Frame.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding BackButtonCommand}"/>
                        </Frame.GestureRecognizers>
                        <Label Text="Back" Style="{StaticResource TileLabelStyle}" FontAttributes="Bold"/>
                    </Frame>
                </Grid>

            </VerticalStackLayout>
        </VerticalStackLayout>
    </ScrollView>
    </Grid>
</ContentPage>
