<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TransactionProcessor.Mobile.Pages.Transactions.Admin.AdminPage"
             xmlns:controls="clr-namespace:TransactionProcessor.Mobile.Controls"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             Shell.NavBarIsVisible="False"
             x:Name="Admin">
    <ScrollView BackgroundColor="{StaticResource pageBackgroundColor}">
        <VerticalStackLayout Spacing="0">

            <!-- Header -->
            <Frame Style="{StaticResource PageHeaderStyle}" BackgroundColor="{StaticResource admin}">
                <Label Text="{Binding Title}" Style="{StaticResource HeaderTitleStyle}"
                       AutomationId="{Binding Title}"/>
            </Frame>

            <VerticalStackLayout Padding="16,16,16,16" Spacing="10">

                <Button AutomationId="ReconciliationButton" x:Name="ReconciliationButton"
                        Text="Reconciliation"
                        Style="{DynamicResource AdminButtonStyle}">
                    <Button.Behaviors>
                        <toolkit:EventToCommandBehavior
                            EventName="Clicked"
                            Command="{Binding ReconciliationCommand}"
                            BindingContext="{Binding Source={x:Reference Admin}, Path=BindingContext}"/>
                    </Button.Behaviors>
                </Button>

                <Button Text="Back" Style="{StaticResource AdminButtonStyle}"
                        Command="{Binding BackButtonCommand}"/>

            </VerticalStackLayout>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
