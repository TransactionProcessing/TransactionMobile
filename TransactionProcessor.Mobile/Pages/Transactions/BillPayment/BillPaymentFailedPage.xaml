<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TransactionProcessor.Mobile.Pages.Transactions.BillPayment.BillPaymentFailedPage"
             xmlns:controls="clr-namespace:TransactionProcessor.Mobile.Controls"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             Shell.NavBarIsVisible="False"
             x:Name="BillPaymentFailed">
    <ScrollView BackgroundColor="{StaticResource pageBackgroundColor}">
        <VerticalStackLayout Spacing="0">

            <!-- Header -->
            <Frame Style="{StaticResource PageHeaderStyle}" BackgroundColor="{StaticResource admin}" AutomationId="{Binding Title}">
                <Label Text="{Binding Title}" Style="{StaticResource HeaderTitleStyle}"/>
            </Frame>

            <VerticalStackLayout Padding="16,24,16,16" Spacing="16">

                <Frame Style="{StaticResource SectionCardStyle}">
                    <VerticalStackLayout Spacing="12" HorizontalOptions="Center">
                        <Image Source="paymentfailure.png" VerticalOptions="Start"
                               HeightRequest="120" WidthRequest="120"
                               HorizontalOptions="Center"/>
                        <Label Text="Payment Failed"
                               FontSize="20" FontAttributes="Bold"
                               TextColor="{StaticResource admin}"
                               HorizontalTextAlignment="Center"/>
                        <Label Text="Unfortunately, we could not process your bill payment. If you wish you can retry the transaction."
                               FontSize="14" TextColor="{StaticResource DarkGray}"
                               HorizontalTextAlignment="Center"
                               Margin="8,0"/>
                    </VerticalStackLayout>
                </Frame>

                <!-- Action tile -->
                <Frame Style="{StaticResource ActionTileStyle}"
                       BackgroundColor="{StaticResource admin}"
                       AutomationId="CancelButton">
                    <Frame.GestureRecognizers>
                        <TapGestureRecognizer Command="{Binding CancelledCommand}"/>
                    </Frame.GestureRecognizers>
                    <Label Text="Cancel" Style="{StaticResource TileLabelStyle}" FontAttributes="Bold"/>
                </Frame>

            </VerticalStackLayout>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
