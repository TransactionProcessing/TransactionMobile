<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             x:Class="TransactionProcessor.Mobile.Pages.Transactions.BillPayment.BillPaymentSuccessPage"
             Shell.NavBarIsVisible="False"
             Padding="0"
             x:Name="BillPaymentSuccess">
    <ScrollView>
        <VerticalStackLayout Spacing="0">

            <!-- Header with gradient background -->
            <Grid HeightRequest="130">
                <Grid.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="{StaticResource billPayment}" Offset="0.0"/>
                        <GradientStop Color="#2b8ccc" Offset="1.0"/>
                    </LinearGradientBrush>
                </Grid.Background>
                <VerticalStackLayout VerticalOptions="Center" HorizontalOptions="Center" Spacing="4" Padding="20">
                    <Label Text="{Binding Title}"
                           AutomationId="{Binding Title}"
                           FontSize="20"
                           FontAttributes="Bold"
                           TextColor="White"
                           HorizontalOptions="Center"/>
                </VerticalStackLayout>
            </Grid>

            <!-- Status card -->
            <VerticalStackLayout Padding="20,20,20,16" Spacing="12">
                <Frame Style="{StaticResource StatusCardFrame}">
                    <VerticalStackLayout Spacing="8" HorizontalOptions="Center">
                        <Image Source="paymentsuccess.svg" VerticalOptions="Start" Style="{DynamicResource PaymentImage}" HorizontalOptions="Center"/>
                        <Label Style="{StaticResource StatusMessageLabel}"
                               Text="Thank you for your bill payment. An automated payment receipt will be sent to your email entered."/>
                    </VerticalStackLayout>
                </Frame>

                <Button x:Name="CompleteButton" Text="Complete"
                        Style="{DynamicResource BillPaymentButtonStyle}" AutomationId="CompleteButton">
                    <Button.Behaviors>
                        <toolkit:EventToCommandBehavior
                            EventName="Clicked"
                            Command="{Binding CompletedCommand}" BindingContext="{Binding Source={x:Reference BillPaymentSuccess}, Path=BindingContext}" />
                    </Button.Behaviors>
                </Button>
            </VerticalStackLayout>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>