<?xml version="1.0" encoding="utf-8" ?>
<common:NoBackWithoutLogoutPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                               xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                               x:Class="TransactionProcessor.Mobile.Pages.Transactions.TransactionsPage"
                               xmlns:common="clr-namespace:TransactionProcessor.Mobile.Pages.Common"
                               xmlns:controls="clr-namespace:TransactionProcessor.Mobile.Controls"
                               xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
                               Shell.NavBarIsVisible="False"
                               x:Name="Transactions">
    <ScrollView BackgroundColor="{StaticResource pageBackgroundColor}">
        <VerticalStackLayout Spacing="0">

            <!-- Header -->
            <Frame Style="{StaticResource PageHeaderStyle}">
                <Label Text="{Binding Title}" Style="{StaticResource HeaderTitleStyle}" AutomationId="Transactions"/>
            </Frame>

            <!-- Transaction type grid -->
            <VerticalStackLayout Padding="16,16,16,16" Spacing="0">

                <Label Text="Select a transaction type" FontSize="14"
                       TextColor="{StaticResource labelTextColor}" Margin="4,0,0,12"/>

                <!-- 2-column grid of transaction type cards -->
                <Grid ColumnDefinitions="*,*" RowDefinitions="auto,auto,auto">

                    <!-- Mobile Topup -->
                    <Frame Grid.Row="0" Grid.Column="0" Style="{StaticResource DashboardTileStyle}"
                           BackgroundColor="{StaticResource mobileTopup}"
                           AutomationId="MobileTopupButton">
                        <Frame.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding MobileTopupCommand}"/>
                        </Frame.GestureRecognizers>
                        <VerticalStackLayout VerticalOptions="Center" HorizontalOptions="Center" Spacing="4">
                            <Label Text="Mobile" Style="{StaticResource TileLabelStyle}" FontAttributes="Bold"/>
                            <Label Text="Topup" Style="{StaticResource TileLabelStyle}"/>
                        </VerticalStackLayout>
                    </Frame>

                    <!-- Mobile Wallet -->
                    <Frame Grid.Row="0" Grid.Column="1" Style="{StaticResource DashboardTileStyle}"
                           BackgroundColor="{StaticResource mobileWallet}"
                           AutomationId="MobileWalletButton">
                        <Frame.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding MobileWalletCommand}"/>
                        </Frame.GestureRecognizers>
                        <VerticalStackLayout VerticalOptions="Center" HorizontalOptions="Center" Spacing="4">
                            <Label Text="Mobile" Style="{StaticResource TileLabelStyle}" FontAttributes="Bold"/>
                            <Label Text="Wallet" Style="{StaticResource TileLabelStyle}"/>
                        </VerticalStackLayout>
                    </Frame>

                    <!-- Bill Payment -->
                    <Frame Grid.Row="1" Grid.Column="0" Style="{StaticResource DashboardTileStyle}"
                           BackgroundColor="{StaticResource billPayment}"
                           AutomationId="BillPaymentButton">
                        <Frame.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding BillPaymentCommand}"/>
                        </Frame.GestureRecognizers>
                        <VerticalStackLayout VerticalOptions="Center" HorizontalOptions="Center" Spacing="4">
                            <Label Text="Bill" Style="{StaticResource TileLabelStyle}" FontAttributes="Bold"/>
                            <Label Text="Payment" Style="{StaticResource TileLabelStyle}"/>
                        </VerticalStackLayout>
                    </Frame>

                    <!-- Voucher -->
                    <Frame Grid.Row="1" Grid.Column="1" Style="{StaticResource DashboardTileStyle}"
                           BackgroundColor="{StaticResource voucher}"
                           AutomationId="VoucherButton">
                        <Frame.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding VoucherCommand}"/>
                        </Frame.GestureRecognizers>
                        <VerticalStackLayout VerticalOptions="Center" HorizontalOptions="Center" Spacing="4">
                            <Label Text="Voucher" Style="{StaticResource TileLabelStyle}" FontAttributes="Bold"/>
                            <Label Text="Issue" Style="{StaticResource TileLabelStyle}"/>
                        </VerticalStackLayout>
                    </Frame>

                    <!-- Admin (full width) -->
                    <Frame Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2"
                           Style="{StaticResource DashboardTileStyle}"
                           BackgroundColor="{StaticResource admin}" HeightRequest="64"
                           AutomationId="AdminButton">
                        <Frame.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding AdminCommand}"/>
                        </Frame.GestureRecognizers>
                        <Label Text="Admin" Style="{StaticResource TileLabelStyle}"
                               FontAttributes="Bold" FontSize="16"
                               VerticalOptions="Center" HorizontalOptions="Center"/>
                    </Frame>

                </Grid>

                <Frame Style="{StaticResource ActionTileStyle}"
                       BackgroundColor="{StaticResource MidGray}"
                       AutomationId="BackButton" Margin="0,12,0,0">
                    <Frame.GestureRecognizers>
                        <TapGestureRecognizer Command="{Binding BackButtonCommand}"/>
                    </Frame.GestureRecognizers>
                    <Label Text="Back" Style="{StaticResource TileLabelStyle}" FontAttributes="Bold"/>
                </Frame>

            </VerticalStackLayout>
        </VerticalStackLayout>
    </ScrollView>
</common:NoBackWithoutLogoutPage>
