<?xml version="1.0" encoding="utf-8" ?>
<common:NoBackWithoutLogoutPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                               xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                               x:Class="TransactionProcessor.Mobile.Pages.Transactions.TransactionsPage"
                               xmlns:common="clr-namespace:TransactionProcessor.Mobile.Pages.Common"
                               Shell.NavBarIsVisible="False"
                               Padding="0"
                               x:Name="Transactions">
    <ScrollView>
        <VerticalStackLayout Spacing="0">

            <!-- Header with gradient background -->
            <Grid HeightRequest="160">
                <Grid.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="{StaticResource primary}" Offset="0.0"/>
                        <GradientStop Color="#2B4A8C" Offset="1.0"/>
                    </LinearGradientBrush>
                </Grid.Background>
                <VerticalStackLayout VerticalOptions="Center" HorizontalOptions="Center" Spacing="6" Padding="20">
                    <Image Source="{AppThemeBinding Light=transactionsbutton.svg, Dark=transactionsbutton_dark.svg}" HeightRequest="60" WidthRequest="60" HorizontalOptions="Center"/>
                    <Label Text="Select Transaction Type"
                           AutomationId="SelectTransactionType"
                           FontSize="20"
                           FontAttributes="Bold"
                           TextColor="White"
                           HorizontalOptions="Center"/>
                </VerticalStackLayout>
            </Grid>

            <!-- Transaction tiles -->
            <VerticalStackLayout Padding="20,24,20,16" Spacing="16">

                <Grid ColumnDefinitions="*,*"
                      RowDefinitions="Auto,Auto,Auto"
                      ColumnSpacing="12"
                      RowSpacing="12">

                    <!-- Mobile Topup tile -->
                    <Frame Grid.Row="0" Grid.Column="0"
                           Style="{StaticResource HomeCardFrame}"
                           AutomationId="MobileTopupButton">
                        <Frame.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding MobileTopupCommand}"/>
                        </Frame.GestureRecognizers>
                        <VerticalStackLayout Spacing="8" HorizontalOptions="Center">
                            <Image Source="{AppThemeBinding Light=transactionsbutton.svg, Dark=transactionsbutton_dark.svg}" HeightRequest="40" WidthRequest="40" HorizontalOptions="Center"/>
                            <Label Text="Mobile Topup"
                                   FontSize="13"
                                   FontAttributes="Bold"
                                   TextColor="{StaticResource mobileTopup}"
                                   HorizontalOptions="Center"
                                   HorizontalTextAlignment="Center"/>
                        </VerticalStackLayout>
                    </Frame>

                    <!-- Mobile Wallet tile -->
                    <Frame Grid.Row="0" Grid.Column="1"
                           Style="{StaticResource HomeCardFrame}"
                           AutomationId="MobileWalletButton">
                        <Frame.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding MobileWalletCommand}"/>
                        </Frame.GestureRecognizers>
                        <VerticalStackLayout Spacing="8" HorizontalOptions="Center">
                            <Image Source="{AppThemeBinding Light=transactionsbutton.svg, Dark=transactionsbutton_dark.svg}" HeightRequest="40" WidthRequest="40" HorizontalOptions="Center"/>
                            <Label Text="Mobile Wallet"
                                   FontSize="13"
                                   FontAttributes="Bold"
                                   TextColor="{StaticResource mobileWallet}"
                                   HorizontalOptions="Center"
                                   HorizontalTextAlignment="Center"/>
                        </VerticalStackLayout>
                    </Frame>

                    <!-- Bill Payment tile -->
                    <Frame Grid.Row="1" Grid.Column="0"
                           Style="{StaticResource HomeCardFrame}"
                           AutomationId="BillPaymentButton">
                        <Frame.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding BillPaymentCommand}"/>
                        </Frame.GestureRecognizers>
                        <VerticalStackLayout Spacing="8" HorizontalOptions="Center">
                            <Image Source="{AppThemeBinding Light=transactionsbutton.svg, Dark=transactionsbutton_dark.svg}" HeightRequest="40" WidthRequest="40" HorizontalOptions="Center"/>
                            <Label Text="Bill Payment"
                                   FontSize="13"
                                   FontAttributes="Bold"
                                   TextColor="{StaticResource billPayment}"
                                   HorizontalOptions="Center"
                                   HorizontalTextAlignment="Center"/>
                        </VerticalStackLayout>
                    </Frame>

                    <!-- Voucher tile -->
                    <Frame Grid.Row="1" Grid.Column="1"
                           Style="{StaticResource HomeCardFrame}"
                           AutomationId="VoucherButton">
                        <Frame.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding VoucherCommand}"/>
                        </Frame.GestureRecognizers>
                        <VerticalStackLayout Spacing="8" HorizontalOptions="Center">
                            <Image Source="{AppThemeBinding Light=transactionsbutton.svg, Dark=transactionsbutton_dark.svg}" HeightRequest="40" WidthRequest="40" HorizontalOptions="Center"/>
                            <Label Text="Voucher"
                                   FontSize="13"
                                   FontAttributes="Bold"
                                   TextColor="{StaticResource voucher}"
                                   HorizontalOptions="Center"
                                   HorizontalTextAlignment="Center"/>
                        </VerticalStackLayout>
                    </Frame>

                    <!-- Admin tile (spans full width) -->
                    <Frame Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2"
                           Style="{StaticResource HomeCardFrame}"
                           AutomationId="AdminButton">
                        <Frame.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding AdminCommand}"/>
                        </Frame.GestureRecognizers>
                        <HorizontalStackLayout Spacing="10" HorizontalOptions="Center">
                            <Image Source="{AppThemeBinding Light=transactionsbutton.svg, Dark=transactionsbutton_dark.svg}" HeightRequest="36" WidthRequest="36" VerticalOptions="Center"/>
                            <Label Text="Admin"
                                   FontSize="15"
                                   FontAttributes="Bold"
                                   TextColor="{StaticResource admin}"
                                   VerticalOptions="Center"/>
                        </HorizontalStackLayout>
                    </Frame>

                </Grid>

                <Button Text="Back"
                        AutomationId="BackButton"
                        Style="{StaticResource StandardButton}"
                        Command="{Binding BackButtonCommand}"/>

            </VerticalStackLayout>
        </VerticalStackLayout>
    </ScrollView>
</common:NoBackWithoutLogoutPage>
