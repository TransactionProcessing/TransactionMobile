<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TransactionProcessor.Mobile.Pages.MyAccount.MyAccountDetailsPage"
             Shell.NavBarIsVisible="False"
             Padding="0">
    <ScrollView>
        <VerticalStackLayout Spacing="0">

            <!-- Header with gradient background -->
            <Grid HeightRequest="130">
                <Grid.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="{StaticResource profile}" Offset="0.0"/>
                        <GradientStop Color="#2aab83" Offset="1.0"/>
                    </LinearGradientBrush>
                </Grid.Background>
                <VerticalStackLayout VerticalOptions="Center" HorizontalOptions="Center" Spacing="4" Padding="20">
                    <Label Text="{Binding Title}"
                           AutomationId="{Binding Title}"
                           FontSize="20"
                           FontAttributes="Bold"
                           TextColor="White"
                           HorizontalOptions="Center"/>
                    <Label Text="Merchant account information"
                           FontSize="13"
                           TextColor="White"
                           Opacity="0.85"
                           HorizontalOptions="Center"/>
                </VerticalStackLayout>
            </Grid>

            <!-- Account details cards -->
            <VerticalStackLayout Padding="20,20,20,16" Spacing="16">

                <!-- Merchant info card -->
                <Frame Style="{StaticResource HomeCardFrame}">
                    <VerticalStackLayout Spacing="12">
                        <Label Text="Merchant Details"
                               FontSize="16"
                               FontAttributes="Bold"
                               TextColor="{StaticResource labelTextColor}"/>

                        <BoxView HeightRequest="1" Color="{StaticResource Gray100}" Margin="0,0,0,4"/>

                        <Grid ColumnDefinitions="Auto,*" RowDefinitions="Auto" ColumnSpacing="12" RowSpacing="8">
                            <Label Grid.Row="0" Grid.Column="0"
                                   Text="Name:"
                                   FontAttributes="Bold"
                                   FontSize="13"
                                   TextColor="{StaticResource labelTextColor}"/>
                            <Label Grid.Row="0" Grid.Column="1"
                                   Text="{Binding MerchantName}"
                                   FontSize="13"
                                   TextColor="{StaticResource labelTextColor}"
                                   AutomationId="MerchantNameLabel"/>
                        </Grid>
                    </VerticalStackLayout>
                </Frame>

                <!-- Balance card -->
                <Frame Style="{StaticResource HomeCardFrame}">
                    <VerticalStackLayout Spacing="12">
                        <Label Text="Balances"
                               FontSize="16"
                               FontAttributes="Bold"
                               TextColor="{StaticResource labelTextColor}"/>

                        <BoxView HeightRequest="1" Color="{StaticResource Gray100}" Margin="0,0,0,4"/>

                        <Grid ColumnDefinitions="*,*" RowDefinitions="Auto,Auto" ColumnSpacing="12" RowSpacing="8">
                            <Label Grid.Row="0" Grid.Column="0"
                                   Text="Balance"
                                   FontAttributes="Bold"
                                   FontSize="12"
                                   TextColor="{StaticResource labelTextColor}"/>
                            <Label Grid.Row="1" Grid.Column="0"
                                   Text="{Binding Balance}"
                                   FontSize="14"
                                   TextColor="{StaticResource profile}"
                                   FontAttributes="Bold"
                                   AutomationId="BalanceLabel"/>

                            <Label Grid.Row="0" Grid.Column="1"
                                   Text="Available Balance"
                                   FontAttributes="Bold"
                                   FontSize="12"
                                   TextColor="{StaticResource labelTextColor}"/>
                            <Label Grid.Row="1" Grid.Column="1"
                                   Text="{Binding AvailableBalance}"
                                   FontSize="14"
                                   TextColor="{StaticResource profile}"
                                   FontAttributes="Bold"
                                   AutomationId="AvailableBalanceLabel"/>
                        </Grid>
                    </VerticalStackLayout>
                </Frame>

                <!-- Statement card -->
                <Frame Style="{StaticResource HomeCardFrame}">
                    <VerticalStackLayout Spacing="12">
                        <Label Text="Statement"
                               FontSize="16"
                               FontAttributes="Bold"
                               TextColor="{StaticResource labelTextColor}"/>

                        <BoxView HeightRequest="1" Color="{StaticResource Gray100}" Margin="0,0,0,4"/>

                        <Grid ColumnDefinitions="Auto,*" RowDefinitions="Auto,Auto,Auto" ColumnSpacing="12" RowSpacing="8">
                            <Label Grid.Row="0" Grid.Column="0"
                                   Text="Last Statement:"
                                   FontAttributes="Bold"
                                   FontSize="13"
                                   TextColor="{StaticResource labelTextColor}"/>
                            <Label Grid.Row="0" Grid.Column="1"
                                   Text="{Binding LastStatementDate, StringFormat='{0:dd/MM/yyyy}'}"
                                   FontSize="13"
                                   TextColor="{StaticResource labelTextColor}"
                                   AutomationId="LastStatementDateLabel"/>

                            <Label Grid.Row="1" Grid.Column="0"
                                   Text="Next Statement:"
                                   FontAttributes="Bold"
                                   FontSize="13"
                                   TextColor="{StaticResource labelTextColor}"/>
                            <Label Grid.Row="1" Grid.Column="1"
                                   Text="{Binding NextStatementDate, StringFormat='{0:dd/MM/yyyy}'}"
                                   FontSize="13"
                                   TextColor="{StaticResource labelTextColor}"
                                   AutomationId="NextStatementDateLabel"/>

                            <Label Grid.Row="2" Grid.Column="0"
                                   Text="Settlement:"
                                   FontAttributes="Bold"
                                   FontSize="13"
                                   TextColor="{StaticResource labelTextColor}"/>
                            <Label Grid.Row="2" Grid.Column="1"
                                   Text="{Binding SettlementSchedule}"
                                   FontSize="13"
                                   TextColor="{StaticResource labelTextColor}"
                                   AutomationId="SettlementScheduleLabel"/>
                        </Grid>
                    </VerticalStackLayout>
                </Frame>

                <Button Text="Edit Details"
                        Style="{StaticResource StandardButton}"
                        AutomationId="EditDetailsButton"/>
                <Button Text="Back"
                        AutomationId="BackButton"
                        Style="{StaticResource StandardButton}"
                        Command="{Binding BackButtonCommand}"/>
            </VerticalStackLayout>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>