<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TransactionProcessor.Mobile.Pages.Reports.ReportsBalanceAnalysisPage"
             xmlns:controls="clr-namespace:TransactionProcessor.Mobile.Controls"
             xmlns:liveChartsCore="clr-namespace:LiveChartsCore.SkiaSharpView.Maui;assembly=LiveChartsCore.SkiaSharpView.Maui"
             Shell.NavBarIsVisible="False">
    <Grid RowDefinitions="auto,*,auto" BackgroundColor="{StaticResource pageBackgroundColor}">

        <!-- Header -->
        <Frame Grid.Row="0" Style="{StaticResource PageHeaderStyle}">
            <Label Text="{Binding Title}" Style="{StaticResource HeaderTitleStyle}"
                   AutomationId="{Binding Title}"/>
        </Frame>

        <!-- Chart -->
        <ScrollView Grid.Row="1">
            <VerticalStackLayout Padding="16,16,16,0">
                <Frame Style="{StaticResource SectionCardStyle}" Padding="8">
                    <liveChartsCore:CartesianChart x:Name="Chart"
                                                   Series="{Binding Series}"
                                                   YAxes="{Binding YAxes}"
                                                   XAxes="{Binding XAxes}"
                                                   TooltipFindingStrategy="{Binding TooltipFindingStrategy}"
                                                   TooltipPosition="{Binding TooltipPosition}"
                                                   HeightRequest="300"
                                                   HorizontalOptions="FillAndExpand"/>
                </Frame>
            </VerticalStackLayout>
        </ScrollView>

        <!-- Back tile -->
        <Frame Grid.Row="2" Style="{StaticResource ActionTileStyle}"
               BackgroundColor="{StaticResource MidGray}"
               AutomationId="BackButton" Margin="16,8">
            <Frame.GestureRecognizers>
                <TapGestureRecognizer Command="{Binding BackButtonCommand}"/>
            </Frame.GestureRecognizers>
            <Label Text="Back" Style="{StaticResource TileLabelStyle}" FontAttributes="Bold"/>
        </Frame>

    </Grid>
</ContentPage>
